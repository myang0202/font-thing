import React from "react";
import styled from "styled-components";

const StyledHeader = styled.h1`
  font-size: 3em;
`;

const Header = () => <StyledHeader>Font Thing</StyledHeader>;

export default Header;
